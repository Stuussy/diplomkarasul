Ты — senior full-stack / mobile разработчик уровня lead.
Твоя задача — аккуратно доработать существующий проект без следов ИИ в коде, комментах и стилях. Пиши так, как пишет живой разработчик: лаконично, без “магии” и воды.
Сейчас нужен частичный объём работ (этап 1) — только три задачи:
Поправить карту/ссылку
Настроить permissions для камеры (QR-сканер)
Ограничить пациенту запись/отмену
1. Карта / ссылка на стоматологию (2GIS)
Проблема:
Карта или ссылка на клинику работает некорректно / не открывается как ожидается.
Нужно, чтобы при нажатии на кнопку/карту открывалось 2GIS с конкретной стоматологией.
Что нужно сделать:
Найти компонент/экран, где отображается клиника и карта (или кнопка “Показать на карте”).
Обеспечить корректное открытие ссылки:
Ссылка на клинику:
https://2gis.kz/astana/search/%D1%81%D1%82%D0%BE%D0%BC%D0%BE%D1%82%D0%BE%D0%BB%D0%BE%D0%B3%D0%B8%D1%8F/filters/sort%3Drating/firm/70000001041371459?m=71.421132%2C51.123988%2F18&immersive=on
Для mobile:
Если есть deep link в 2GIS — попытаться открыть его через Linking / соответствующий механизм.
Если приложения 2GIS нет или deep link не сработал — открыть ссылку в браузере.
Обработать ошибку: показать пользователю понятное сообщение, если ссылка не открывается.
Для web:
Открывать ссылку в новой вкладке (target="_blank" или аналог).
Убедиться, что URL корректно закодирован и не ломается при навигации/роутинге.
Вынести URL и, при необходимости, deep-link в конфиг/константы (не хардкодить по всему коду).
Добавить минимальные логирования (если есть общий логгер): успешное открытие / ошибка открытия карты.
2. Permissions для камеры (скан QR-кода)
Проблема:
Не работает камера для сканирования QR-кода, вероятнее всего из-за не настроенных разрешений или некорректной инициализации.
Что нужно сделать:
Найти экран/компонент, который отвечает за сканирование QR-кода (например, QrScanner, ScanScreen, и т.п.).
Реализовать/исправить запрос разрешений на камеру:
Для Android:
Проверить, что в манифесте есть нужные разрешения (CAMERA).
В рантайме запрашивать разрешение перед стартом сканера.
Обработать сценарии:
разрешение выдано → запуск сканера;
отказ → показать понятное сообщение (“Чтобы сканировать код, нужен доступ к камере”) и не падать.
Для iOS:
Проверить Info.plist (описание причины доступа к камере).
Аналогично: запросить разрешение, аккуратно обработать отказ.
Для web (если актуально):
Использовать navigator.mediaDevices.getUserMedia({ video: true }) с обработкой ошибок:
нет устройства камеры;
запрет от пользователя;
ошибка браузера.
При отказе — выводить адекватное сообщение и не ломать страницу.
Инициализация сканера:
Запускать сканер только после получения разрешения.
При закрытии экрана/компонента останавливать поток камеры, чтобы избежать утечек и зависаний.
UX-мелочи:
При первом входе на экран — короткое текстовое объяснение, зачем нужен доступ к камере.
Если пользователь навсегда запретил разрешение (например, “Не спрашивать снова”) — предложить открыть настройки устройства/браузера.
Код должен быть:
сдержан по логам,
без “ИИ-комментариев”,
без лишних абстракций — только необходимый минимум.
3. Ограничение записи/отмены для пациента
Проблема:
Сейчас пациент, вероятно, может:
записаться несколько раз на один и тот же слот/к одному врачу;
отменять запись без подтверждения;
отсутствует чёткий индикатор “вы уже записаны”.
Что нужно сделать:
3.1. Ограничение “один пользователь — одна запись (на слот)”
На backend:
В модели записей (appointments/records) реализовать проверку:
при создании записи проверять, нет ли уже активной записи этого пользователя:
либо на конкретное время/слот,
либо к этому врачу на выбранный временной интервал — в зависимости от логики проекта.
Если запись уже существует:
возвращать понятную ошибку (например, код/тип already_booked).
На frontend:
При получении ошибки already_booked:
показывать пользователю сообщение:
“Вы уже записаны”.
Если пользователь уже записан:
вместо активной кнопки “Записаться” — либо disabled-состояние, либо метка/кнопка “Вы уже записаны”.
не давать повторно отправлять запрос на запись.
Состояние UI:
При успешной записи:
обновить локальный стейт/кеш так, чтобы сразу отражалось, что запись есть.
При загрузке экрана:
подтягивать данные о текущих записях пользователя и на их основе выставлять состояние “уже записан”.
3.2. Подтверждение отмены записи
Перед отменой записи:
показывать модальное окно (или диалог подтверждения) с текстом:
Заголовок:
“Отменить запись?”
Текст:
“Вы уверены, что хотите отменить запись?”
Кнопки:
Основная (опасная): “Да, отменить”
Вспомогательная: “Отмена”
Если пользователь подтвердил:
вызываем API отмены (backend);
после успешного ответа:
обновляем список записей;
возвращаем интерфейс в состояние “можно записаться”.
Если пользователь отменил диалог:
ничего не делаем, UI остаётся как есть.
Не забыть:
обрабатывать ошибки от backend (недоступность сервера, ошибки валидации);
показывать пользователю понятные короткие сообщения;
не использовать формулировки вида “что-то пошло не так” без деталей.